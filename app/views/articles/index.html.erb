<body>
    <div class="bonito">
      <h1>Articles</h1>
      <ul>
        <% @articles.each do |article| %>
        <% @user = User.find(article.user_id) %>
        <li>
          <%= article.title %>
          <%= @user.name %>
        </li>
      <% end %>
      </ul>
      <a href="http://localhost:3000/articles3"><button>Anterior</button></a>
      <a href="http://localhost:3000/articles4"><button>Começo</button></a>
    </div> 
</body>
</html>
<style>
    .bonito{
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    .bonito > a button{
        margin-bottom: 20px;
        background: #069cc2;
        border-radius: 6px;
        padding: 15px;
        cursor: pointer; 
        color: #fff; 
        border: none; 
        font-size: 16px;
    }
</style>


